#include "main.h"

/**
 * add - take two numbers and return them added
 * Return: a sum
 * On error, -1 is returned, and errno is set appropriately.
 * @n1: an integer
 * @n2: an integer
 */
int add(int n1, int n2)
{
	return (n1 + n2);
}
