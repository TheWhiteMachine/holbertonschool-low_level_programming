#include "main.h"

/**
 * _isdigit - check the code.
 *
 * Return: 1 for c as a digit and 0 c to not.
 * @c: an integer number
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
		return (1);
	else 
		return (0i);
}
