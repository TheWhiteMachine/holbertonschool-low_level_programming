#include "main.h"

/**
 * mul - check the code.
 *
 * Return: the result of a and b multiplication.
 * @a: an integer number
 * @b: an integer number
 */

int mul(int a, int b)
{
	int r = a * b;

	return (r);
}
